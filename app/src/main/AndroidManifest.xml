<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apps.amazon.com.dide">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.SignupActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.TwinActivity"
            android:label="@string/title_activity_twin"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.EditActivity" />
        <activity android:name=".activities.ViewPostActivity" />
        <activity android:name=".activities.AddNewStory" />
        <activity android:name=".activities.UrgentEmergency" />
        <activity android:name=".activities.DefensePostureActivity" />
        <activity android:name=".activities.DefendingFromFrontActivity" />
        <activity android:name=".activities.DefendingYourBackActivity" />
        <activity android:name=".activities.AvoidingConfrontationActivity" />
        <activity android:name=".activities.QuotesActivity" />
        <activity android:name=".activities.InterludeActivity"></activity>
    </application>

</manifest>